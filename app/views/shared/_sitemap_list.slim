ul.sitemap-category-list
  - items.each do |item|
    li.sitemap-category-item
      = link_to fa_icon('chevron-right', text: item.title), item, class: 'internal navigation-lines-link sitemap-link', data: { :'page-slug' => "page-id-#{item.id}", color: color }
